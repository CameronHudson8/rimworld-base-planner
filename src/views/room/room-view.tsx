import { ReactElement } from "react";

export interface RoomViewProps {
  color: string;
  id: string;
  // links: { name: string }[];
  name: string;
  size: number;
}


export function RoomView(): ReactElement {
  return (<></>);
}
